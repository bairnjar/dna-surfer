using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public enum PATHTYPE { RIGHT, LEFT, BOTH }


public class DnaInfo : MonoBehaviour
{
    public PATHTYPE entryType;
    public PATHTYPE exitType;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
